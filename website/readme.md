# Website

You need to run the website over a server otherwise it dosen't work on Linux. On Windows it **should** work fine. Not tested on mac

## Test Server

```powershell
python -m http.server 
```

```powershell
npx light-server -s . -p 8080
```

## Bootstrap icons

https://icons.getbootstrap.com/
